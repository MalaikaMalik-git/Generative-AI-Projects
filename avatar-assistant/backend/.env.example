APP_ENV=development

# Required for the /chat and /vision endpoints (gpt-4o-mini handles both).
# Get a key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Only needed once deployed — your deployed frontend's URL, so CORS
# allows it. Leave blank for local development.
FRONTEND_URL=
